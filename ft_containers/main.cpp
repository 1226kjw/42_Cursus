#include <iostream>
#include <vector>
#include "vector.hpp"
#include "stack.hpp"
#include "map.hpp"
#include <map>
using std::cout;
using std::endl;

int main()
{
	ft::map<int, int> a;
	//ft::vector<int> b(a.begin(), a.end());

	// for (size_t i = 0; i < b.size(); ++i)
	// 	cout << b[i] << '\n';
	// ft::stack<int> aa;
	// aa.push(10);
	// aa.push(0);
	// aa.push(3);
	// aa.push(4);
	// aa.push(7);
	// ft::stack<int> bb;
	// bb.push(10);
	// while (!aa.empty())
	// {
	// 	cout << aa.top() << endl;
	// 	cout << (aa >= bb) << endl;
	// 	aa.pop();
	// }
	return 0;
}
